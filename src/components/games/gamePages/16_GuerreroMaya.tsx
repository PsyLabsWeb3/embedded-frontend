import React from "react";
import GamePage from "../GamePage";

const GuerreroMaya: React.FC = () => {
  return <GamePage gameId="16-guerrero-maya" customContent={<div></div>} />;
};

export default GuerreroMaya;
