import React from "react";
import GamePage from "../GamePage";

const EndlessRunner: React.FC = () => {
  return <GamePage gameId="15-endless-runner" customContent={<div></div>} />;
};

export default EndlessRunner;
