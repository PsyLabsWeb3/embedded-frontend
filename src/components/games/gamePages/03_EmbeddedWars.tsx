import React from 'react';
import GamePage from '../GamePage';

const BubbleMerge: React.FC = () => {
  return (
    <GamePage 
      gameId="03-Embedded-Wars"
      customContent={<div></div>}
    />
  );
};

export default BubbleMerge;
